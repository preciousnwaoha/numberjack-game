

export const envs = {
    SERVER_URL: process.env.NEXT_PUBLIC_SERVER_URL as string,
    CONTRACT_ADDRESS: process.env.NEXT_PUBLIC_CONTRACT_ADDRESS as string,
    TOKEN_CONTACT_ADDRESS: process.env.NEXT_PUBLIC_TOKEN_CONTACT_ADDRESS as string,
}